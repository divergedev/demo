root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = ["go", "html", "js", "css"]
  exclude_dir = ["tmp", "vendor", "node_modules", "dist"]
  delay = 1000

[log]
  time = false
